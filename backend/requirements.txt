# ── Бэкенд ───────────────────────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9      # multipart-загрузка файла в POST /api/jobs
pydantic>=2.7

# ── LLM ──────────────────────────────────────────────────────────────────────
openai>=1.30                 # клиент к DeepSeek через base_url

# ── STT + диаризация (ядро переиспользовано из voice-recognition-bot) ────────
faster-whisper>=1.0
pyannote.audio>=3.1          # на Python 3.13 встаёт ветка 4.x
torch>=2.0
torchaudio>=2.0
scipy>=1.11                  # чтение wav в память, минуя torchcodec

# ── Прочее ───────────────────────────────────────────────────────────────────
numpy>=1.26
